export interface CarSpinConfig {
  images: string[]
  autoplay: boolean
  autoplaySpeed: number
  dragSpeed: number
  fullscreen: boolean
  magnifier: number
}

export interface CarSpinMedia {
  id: string
  frames: string[]
  frameCount: number
  sort_order: number
}
